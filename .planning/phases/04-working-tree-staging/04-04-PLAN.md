---
phase: 04-working-tree-staging
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - src/App.svelte
autonomous: false
requirements:
  - STAGE-01
  - STAGE-02
  - STAGE-03
  - STAGE-04

must_haves:
  truths:
    - "StagingPanel appears as the third pane in the 3-column layout (branch sidebar | commit graph | staging panel)"
    - "User can see unstaged and staged file lists with colored status icons"
    - "Clicking the hover action button on a file row stages or unstages the file"
    - "Stage All Changes and Unstage All buttons work correctly"
    - "Modifying a file in the terminal causes the panel to refresh automatically within ~300ms"
    - "Panel shows 'Unstaged Files (0)' and 'Staged Files (0)' when working tree is clean"
    - "The commit graph and branch sidebar still function correctly alongside the new panel"
  artifacts:
    - path: "src/App.svelte"
      provides: "3-pane layout with StagingPanel wired in, phase 4 comment replaced"
      contains: "StagingPanel"
  key_links:
    - from: "src/App.svelte"
      to: "StagingPanel.svelte"
      via: "<StagingPanel repoPath={repoPath!} />"
      pattern: "StagingPanel"
    - from: "StagingPanel.svelte"
      to: "repo-changed Tauri event"
      via: "listen('repo-changed') $effect"
      pattern: "repo-changed"
---

<objective>
Wire StagingPanel into App.svelte, replacing the `<!-- Phase 4 adds StagingPanel here -->` comment, and run the visual verification checkpoint to confirm the full staging workflow works end-to-end.

Purpose: Completes Phase 4 by connecting all the pieces — Rust commands, watcher, and Svelte components — into the live app. The human verification confirms staging actions and auto-refresh work as specified.

Output: Updated src/App.svelte with StagingPanel as the third pane; human confirmation that the feature works correctly.
</objective>

<execution_context>
@/Users/joaofnds/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-working-tree-staging/04-CONTEXT.md
@.planning/phases/04-working-tree-staging/04-03-SUMMARY.md

<interfaces>
<!-- Current App.svelte structure for reference. -->

Current src/App.svelte layout (the comment is the insertion point):
```svelte
<main class="flex-1 overflow-hidden flex">
  <BranchSidebar repoPath={repoPath!} onrefreshed={handleRefresh} />
  <div class="flex-1 overflow-hidden">
    {#key graphKey}
      <CommitGraph {repoPath} />
    {/key}
  </div>
  <!-- Phase 4 adds StagingPanel here -->
</main>
```

StagingPanel props (from Plan 03):
```typescript
interface Props {
  repoPath: string;
  currentBranch?: string;
}
```

BranchSidebar already loads refs and calls onrefreshed. To get currentBranch for the panel header, App.svelte can:
- Accept a prop bubbled up from BranchSidebar via a new callback, OR
- Keep it simple: pass repoPath only and let StagingPanel derive the branch name from the status fetch result (it already has the repo path to call list_refs if needed), OR
- Simplest: add an `oncurrentbranch` callback to BranchSidebar that bubbles the active branch name to App.svelte

Claude's discretion: Use the simplest approach. Recommended: let StagingPanel call list_refs internally to get the branch name, keeping App.svelte changes minimal (import + mount only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire StagingPanel into App.svelte</name>
  <files>src/App.svelte</files>
  <action>
    1. Add import at top of script block: `import StagingPanel from './components/StagingPanel.svelte';`

    2. Replace the comment `<!-- Phase 4 adds StagingPanel here -->` with:
       ```svelte
       <StagingPanel repoPath={repoPath!} />
       ```

    3. The currentBranch prop: StagingPanel handles this internally by calling list_refs when it loads status (it has repoPath). No App.svelte state changes needed for currentBranch.

    4. Ensure the 3-pane layout maintains correct proportions:
       - BranchSidebar: fixed width (as currently set, likely ~220px)
       - CommitGraph: flex-1 (grows to fill space)
       - StagingPanel: fixed width 240px (set inside the component)

    The `<div class="flex-1 overflow-hidden">` wrapper around CommitGraph ensures the graph doesn't shrink to zero with the staging panel present — this flex-1 wrapper already exists and is correct.

    Verify `bun run check` passes before moving to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && bun run check 2>&1 | tail -20</automated>
  </verify>
  <done>App.svelte imports and mounts StagingPanel; `bun run check` clean; phase 4 comment replaced</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of staging panel end-to-end</name>
  <action>
    Human verifies the staging panel works correctly end-to-end in `bun run tauri dev`.

    What was built:
    - Rust: get_status, stage_file, unstage_file, stage_all, unstage_all commands
    - Rust: filesystem watcher with 300ms debounce emitting 'repo-changed'
    - Svelte: StagingPanel with two collapsible sections, colored status icons, hover action buttons, silent auto-refresh
    - App: 3-pane layout (branch sidebar | commit graph | staging panel)
  </action>
  <verify>
    Run the app: `bun run tauri dev`

    Open any local git repository with changes. Then verify all 7 checks:

    1. **Panel appears**: Staging panel is visible as the third column on the right. Branch sidebar | commit graph | staging panel layout.

    2. **Status display (STAGE-01)**: Files appear in "Unstaged Files (N)" and "Staged Files (N)" sections. Each file shows a colored icon (green + for new, orange pencil for modified, red - for deleted, blue arrow for renamed). Relative paths shown for subdirectory files.

    3. **Individual stage/unstage (STAGE-02)**: Hover over a file in the Unstaged list. A small action button (+) appears on the right. Click it. The file moves to the Staged list. Hover over the now-staged file. Click the (-) button. The file moves back to Unstaged.

    4. **Bulk actions (STAGE-03)**: Click "Stage All Changes" - all unstaged files move to staged. Click "Unstage All" - all staged files return to unstaged.

    5. **Auto-refresh (STAGE-04)**: While the panel is open, modify a file from the terminal (e.g., `echo "test" >> somefile.txt`). Within approximately 300ms, the staging panel updates automatically without any user action.

    6. **Clean working tree**: In a clean repo (no changes), both "Unstaged Files (0)" and "Staged Files (0)" section headers are visible. Lists are empty below them.

    7. **Existing features**: Branch sidebar still works (lists branches, checkout works). Commit graph still scrolls and shows commits correctly.

    If any check fails, note the specific failure so it can be fixed.
  </verify>
  <done>All 7 checks pass; human types "approved" to continue</done>
  <resume-signal>Type "approved" if all 7 checks pass, or describe specific failures for each check that failed.</resume-signal>
</task>

</tasks>

<verification>
Visual verification by human after `bun run tauri dev`. All 7 checks in the checkpoint must pass:
1. Panel appears in correct position
2. Status icons and file lists correct
3. Individual stage/unstage works
4. Stage All / Unstage All works
5. Auto-refresh fires within ~300ms
6. Clean repo shows both sections at (0)
7. Existing features unbroken
</verification>

<success_criteria>
- App.svelte: StagingPanel imported and mounted, phase 4 comment removed
- `bun run check` passes
- Human verifies all 7 staging behaviors work in `bun run tauri dev`
- Phase 4 is functionally complete: STAGE-01, STAGE-02, STAGE-03, STAGE-04 all working
</success_criteria>

<output>
After completion, create `.planning/phases/04-working-tree-staging/04-04-SUMMARY.md`
</output>

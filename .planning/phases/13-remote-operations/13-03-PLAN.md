---
phase: 13-remote-operations
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/components/Toolbar.svelte, src/components/StatusBar.svelte]
autonomous: true
gap_closure: true
requirements: [REMOTE-01, REMOTE-02, REMOTE-03, REMOTE-04]

must_haves:
  truths:
    - "Clicking Stash saves current working changes"
    - "Clicking Pop applies the top stash entry"
    - "Cancel button is visually adjacent to progress text during remote operations"
  artifacts:
    - path: "src/components/Toolbar.svelte"
      provides: "Stash and Pop button handlers with correct IPC parameters"
      contains: "message:"
    - path: "src/components/StatusBar.svelte"
      provides: "Cancel button positioned next to progress text"
  key_links:
    - from: "src/components/Toolbar.svelte"
      to: "stash_save command"
      via: "safeInvoke with message param"
      pattern: "safeInvoke.*stash_save.*message"
    - from: "src/components/Toolbar.svelte"
      to: "stash_pop command"
      via: "safeInvoke with index param"
      pattern: "safeInvoke.*stash_pop.*index"
---

<objective>
Fix two UAT gaps from Phase 13: (1) Stash/Pop toolbar buttons silently fail due to missing IPC parameters, and (2) cancel button is pushed too far right in StatusBar during remote operations.

Purpose: Close all Phase 13 UAT failures so the phase passes acceptance testing.
Output: Corrected Toolbar.svelte and StatusBar.svelte files.
</objective>

<execution_context>
@/Users/joaofnds/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-remote-operations/13-02-SUMMARY.md

<interfaces>
<!-- Rust backend command signatures that frontend must match -->

From src-tauri/src/commands/stash.rs:
```rust
#[tauri::command]
pub async fn stash_save(path: String, message: String, ...) -> Result<(), String>

#[tauri::command]
pub async fn stash_pop(path: String, index: usize, ...) -> Result<(), String>
```

From src/lib/invoke.ts:
```typescript
export function safeInvoke<T>(cmd: string, args?: Record<string, unknown>): Promise<T>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Stash and Pop button handlers with correct IPC parameters</name>
  <files>src/components/Toolbar.svelte</files>
  <action>
In src/components/Toolbar.svelte, fix the two broken handlers:

1. handleStash (line ~41): Add `message: ''` parameter to safeInvoke call.
   Change: `await safeInvoke('stash_save', { path: repoPath })`
   To: `await safeInvoke('stash_save', { path: repoPath, message: '' })`

2. handlePop (line ~49): Add `index: 0` parameter to safeInvoke call.
   Change: `await safeInvoke('stash_pop', { path: repoPath })`
   To: `await safeInvoke('stash_pop', { path: repoPath, index: 0 })`

3. Replace the empty catch blocks in both handlers with console.error logging:
   Change: `catch { // stash errors are non-fatal for UI }`
   To: `catch (e) { console.error('stash_save failed:', e); }`
   And: `catch (e) { console.error('stash_pop failed:', e); }`

The Rust backend requires `message: String` for stash_save and `index: usize` for stash_pop. The current calls omit these, causing Tauri IPC deserialization errors that the empty catch blocks silently swallow.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && grep -n "message:" src/components/Toolbar.svelte && grep -n "index:" src/components/Toolbar.svelte && grep -n "console.error" src/components/Toolbar.svelte</automated>
  </verify>
  <done>handleStash sends message: '' param, handlePop sends index: 0 param, both catch blocks log errors to console</done>
</task>

<task type="auto">
  <name>Task 2: Reposition cancel button adjacent to progress text in StatusBar</name>
  <files>src/components/StatusBar.svelte</files>
  <action>
In src/components/StatusBar.svelte, fix the cancel button position so it sits right next to the progress text instead of being pushed to the far right of the status bar.

1. Remove `flex: 1` from the `.status-text` CSS rule (line ~139). The status-text span should NOT greedily fill remaining space. Instead it should only take the width of its content.

2. Keep all other .status-text properties (overflow, text-overflow, white-space) intact. Add `min-width: 0` to ensure text truncation still works when the bar is narrow.

The layout should be: [spinner] [progress text] [cancel X] ... [empty space]. The cancel button must be immediately next to the progress text, not at the far right edge.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && grep -A5 "status-text" src/components/StatusBar.svelte | grep -v "flex: 1" && echo "flex: 1 removed"</automated>
  </verify>
  <done>Cancel button renders immediately adjacent to progress text during remote operations, not pushed to far right edge of status bar</done>
</task>

</tasks>

<verification>
1. `cd /Users/joaofnds/code/trunk && cargo tauri build --debug 2>&1 | tail -5` compiles without errors
2. Toolbar.svelte handleStash includes `message: ''` in safeInvoke args
3. Toolbar.svelte handlePop includes `index: 0` in safeInvoke args
4. StatusBar.svelte .status-text does NOT have `flex: 1`
</verification>

<success_criteria>
- Stash button triggers stash_save successfully (no silent IPC error)
- Pop button triggers stash_pop successfully (no silent IPC error)
- Cancel button appears adjacent to progress text during remote operations
- Both catch blocks log errors to console instead of swallowing silently
</success_criteria>

<output>
After completion, create `.planning/phases/13-remote-operations/13-03-SUMMARY.md`
</output>

# A test that stops making progress must fail, not hang a CI runner. The
# slowest honest test is ~40s, so 120s only fires on something stuck.
[profile.default]
slow-timeout = { period = "30s", terminate-after = 4 }
